# Redroid

The Compose service uses `${REDROID_IMAGE}` from `.env`.

For reliable push delivery, choose an amd64 Redroid image with Google Play Services / FCM support. The project does not ship GApps or Google APKs.
